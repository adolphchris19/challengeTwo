<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.randaquiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lunch"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChemistryActivity"
            android:label="@string/category_chemistry" />
        <activity
            android:name=".PhysicsActivity"
            android:label="@string/category_physics" />
        <activity
            android:name=".EnglishActivity"
            android:label="@string/category_english" />
        <activity
            android:name=".MathematicsActivity"
            android:label="@string/category_mathematics">

        </activity>
        <activity android:name=".AboutMe" android:label="About Me">

        </activity>
    </application>

</manifest>